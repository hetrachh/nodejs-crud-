# Blog API

# Instalation

1. Download NodejS
2. Download XAMPP or install PHP my admin
3. run command
   1. npm install
   2. npx sequelize-cli db:migrate:all
   3. npx sequelize-cli db:seed:all
4. copy .env.example past as .env and set secret value
5. To run npm start
